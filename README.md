# Local-RAG
A RAG (Retrieval-Augmented Generation) system that runs entirely locally using quantized LLMs (via Ollama). Upload documents and ask questions – no API keys, no cloud costs, no internet required after initial setup.
